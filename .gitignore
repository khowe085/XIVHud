# Claude Code local settings (personal, not shared)
.claude/settings.local.json

# Claude memory (personal, generated at runtime)
.claude/memory/
.claude/worktrees/

# Windower addon user settings — created at runtime, never committed
*/data/
# Extracted item icons, written by equipviewer at runtime beside the addon.
/src/icons/

# Packaging output (see .github/workflows/package.yml)
/dist/
/*.zip

# Runtime load trace written by src/XIVHud.lua
load.log
